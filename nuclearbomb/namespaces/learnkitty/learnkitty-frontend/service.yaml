apiVersion: v1
kind: Service
metadata:
  name: learnkitty-frontend
  namespace: learnkitty
spec:
  selector:
    app: learnkitty-frontend
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
  type: ClusterIP